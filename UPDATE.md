# Code Quest 안전 업데이트 안내

이 문서는 이미 Code Quest를 사용 중인 사람이 새 `code-quest.zip`으로 업데이트할 때 기존 설정과
학습 데이터를 최대한 안전하게 보존하는 절차입니다.

> **가장 중요한 원칙:** 새 ZIP을 기존 Code Quest 폴더 위에 덮어쓰지 마세요. 기존 폴더는
> 기존 파일 보관용으로 남기고, 새 폴더에 압축을 푼 뒤 기존 `.env`만 그대로 복사합니다.

릴리스마다 문제 구성이나 데이터베이스 구조가 달라질 수 있습니다. 배포 안내에 지원하는 이전
버전과 진도 영향이 적혀 있지 않다면 학습 진도 호환성이 확인된 업데이트로 간주하지 마세요.

기존 폴더에는 `.env`와 사용자가 수정한 파일이 남지만, 서버 진도 데이터는 별도의 Docker
volume에 있습니다. 따라서 기존 폴더 보관은 데이터베이스 백업이나 다운그레이드 보장이 아닙니다.

이 절차는 같은 컴퓨터의 같은 Docker Desktop 또는 Docker Engine에서 Code Quest의 기본 시작
파일을 계속 사용하는 경우를 기준으로 합니다. `docker compose -p`나 `COMPOSE_PROJECT_NAME`으로
프로젝트 이름을 직접 바꿨거나 다른 Docker context를 사용했다면 같은 volume을 선택한다고
보장할 수 없으므로 먼저 개발자에게 확인하세요.

## 무엇이 어디에 저장되나요?

| 항목 | 저장 위치 | 업데이트 시 필요한 조건 |
|---|---|---|
| 정답 여부와 시도 횟수 | Docker의 `code-quest_codequest-data` volume | volume을 삭제하지 않고, 기존 `.env`와 호환되는 새 버전을 사용 |
| 데이터베이스 비밀번호와 runner token | Code Quest 폴더의 `.env` | 기존 파일을 수정하지 않고 새 폴더로 복사 |
| `.env.backup-*`과 사용자가 수정한 파일 | 기존 Code Quest 폴더 | 기존 폴더를 삭제하지 않고 보관 |
| 답안 초안, 마지막 위치와 학습자 식별값 | 브라우저의 `http://localhost:3000` 저장 공간 | 같은 브라우저 프로필과 같은 주소를 계속 사용 |

Docker volume에 진도가 남아 있어도 브라우저의 학습자 식별값이 바뀌면 기존 진도에 접근하지
못할 수 있습니다. 브라우저 데이터를 지우거나 시크릿 창·다른 브라우저 프로필로 바꾸지 말고,
`http://127.0.0.1:3000` 대신 기존과 같은 <http://localhost:3000>을 사용하세요.

문제의 채점 기준이나 번호가 바뀐 릴리스는 해당 문제의 진도를 의도적으로 초기화할 수 있습니다.
따라서 volume과 브라우저 데이터가 남아 있다는 사실만으로 모든 문제의 진도 보존을 보장하지는
않습니다. HTML·CSS 문제 카탈로그 revision이 바뀌면 기존 답안 초안 값이 브라우저에 남아 있어도
새 화면에서 자동으로 불러오지 않을 수 있습니다. 제출한 답안 원문은 서버 데이터베이스에 별도로
저장되지 않습니다.

## 시작 전에 확인할 것

이 절차는 폴더 덮어쓰기와 `.env` 손실을 막지만, 특정 두 버전 사이의 데이터 호환성을 증명하지는
않습니다. 배포 안내가 현재 사용 중인 배포본을 업데이트 대상으로 명시하고 진도 영향을 설명한
경우에만 진행하세요.

기존 설치 폴더에는 source commit을 식별하는 파일이 없을 수 있습니다. 원본 ZIP을 보관하고 있다면
배포 안내의 ZIP 이름·SHA-256과 비교하고, 원본 ZIP이나 배포 정보를 찾을 수 없다면 파일 수정
시각으로 추측하지 말고 배포자에게 먼저 확인하세요.

버전 식별자가 없는 기존 폴더를 이 문서만으로 확정하는 방법은 아직 없습니다. 확인이 막히는
경우는 안전을 위한 중단 조건이며, 아래 절차만 보고 임의로 계속 진행하면 안 됩니다. 이후
배포본을 자동 식별하는 release manifest는 별도 자동화 작업으로 추가해야 합니다.

## 안전한 업데이트 순서

아래 예시는 기존 폴더를 날짜가 포함된 `code-quest-old-2026-08-01` 같은 이름으로 바꾸고, 새
ZIP에서 만들어진 폴더를 `code-quest`로 사용하는 절차입니다. 같은 이름이 이미 있으면 끝에
`-2`처럼 번호를 붙여 기존 폴더를 덮어쓰지 마세요.

시작하기 전에 현재 화면에서 알고 있는 완료 문제와 시도 횟수를 확인해 두면 업데이트 후 결과를
비교하기 쉽습니다.

### 1. 기존 Code Quest를 안전하게 종료합니다

Docker Desktop이 실행 중인 상태에서 **기존 Code Quest 폴더**의 터미널을 열고 다음 명령을
실행합니다.

```bash
docker compose down
```

이 명령은 컨테이너를 종료하지만 학습 진도 volume은 삭제하지 않습니다. 다음 명령은 사용하지
마세요.

```text
docker compose down --volumes
```

실행 중인 컨테이너를 둔 채 파일을 교체하거나 새 버전을 동시에 시작하지 마세요.

명령이 끝나면 터미널이나 PowerShell 창을 닫으세요. 특히 Windows에서는 터미널이 기존 폴더를
사용 중이면 폴더 이름을 바꿀 수 없습니다.

### 2. 기존 폴더를 보관용 이름으로 바꿉니다

기존 `code-quest` 폴더를 날짜나 번호가 포함된 고유한 이름으로 바꿉니다. 이 폴더를 복사하거나
삭제할 필요는 없습니다. 다음 항목이 그대로 남아 있어야 합니다.

- `.env`
- 존재한다면 `.env.backup-*`
- 사용자가 직접 수정하거나 추가한 파일

`.env`에는 로컬 비밀번호와 token이 있으므로 내용을 화면에 출력하거나 다른 사람에게 보내지
마세요.

### 3. 새 ZIP을 별도의 새 폴더로 압축 해제합니다

- macOS: ZIP을 기존 보관 폴더의 **상위 폴더**에서 더블클릭합니다.
- Windows: `모두 추출`을 선택하고 기존 보관 폴더의 **상위 폴더**를 대상 위치로 선택합니다.
- Linux: 기존 보관 폴더의 상위 폴더에서 `unzip code-quest.zip`을 실행합니다.

ZIP 자체에 `code-quest/` 최상위 폴더가 들어 있습니다. 이미 만든 `code-quest` 폴더 안에 다시
압축을 풀면 `code-quest/code-quest`처럼 중첩될 수 있습니다.

새 폴더를 열었을 때 다음 파일이 바로 보여야 합니다.

```text
code-quest/
├── START_HERE.md
├── UPDATE.md
├── start.command
├── start.cmd
├── start.ps1
├── start.sh
└── docker-compose.yml
```

### 4. 기존 `.env`만 새 폴더로 복사합니다

기존 `.env`의 값을 새로 만들거나 직접 입력하지 말고 파일 전체를 그대로 복사합니다.

- macOS Finder: 기존 폴더에서 `Command + Shift + .`을 눌러 숨김 파일을 표시한 뒤 `.env`를
  `Command + C`로 복사하고 새 폴더에서 `Command + V`로 붙여넣습니다.
- Windows 파일 탐색기: 기존 폴더의 `.env`를 `Ctrl + C`로 복사하고 새 폴더에서 `Ctrl + V`로
  붙여넣습니다.
- Linux 파일 관리자: `Ctrl + H`로 숨김 파일을 표시한 뒤 `.env`를 복사해 새 폴더에
  붙여넣습니다.

`.env.backup-*`은 기존 보관 폴더에 그대로 두면 보존됩니다. `.env`가 보이지 않거나 없다면 새
시작 파일을 실행하지 말고 기존 `.env.backup-*` 복원 방법을 배포자에게 문의하세요.

`.env.example`을 대신 복사하거나 `POSTGRES_PASSWORD` 같은 일부 값만 옮기지 마세요. Windows에서
메모장으로 다시 만들면 `.env.txt`가 될 수 있으므로 반드시 기존 파일 자체를 복사하세요. 새
시작 파일은 `.env` 복사가 끝난 뒤 처음 실행합니다.

기존 폴더의 다른 파일을 새 폴더 전체에 복사하지 마세요. 구버전에서 삭제되거나 이름이 바뀐
파일이 남아 새 버전과 섞일 수 있습니다.

### 5. 새 버전을 실행합니다

Docker Desktop이 실행 중인지 확인한 뒤 새 폴더에서 운영체제에 맞는 시작 파일을 실행합니다.

| 운영체제 | 실행 방법 |
|---|---|
| macOS | `start.command` 더블클릭 또는 `bash start.sh` |
| Windows | `start.cmd` 더블클릭 |
| Linux | `bash start.sh` |

macOS·Linux 압축 해제 과정에서 실행 권한이 사라져도 `bash start.sh`를 사용하면 됩니다.

### 6. 업데이트 결과를 확인합니다

같은 브라우저 프로필에서 <http://localhost:3000>을 열고 다음을 확인합니다.

- Code Quest 화면이 정상적으로 열리는가
- 기존 정답 여부와 시도 횟수가 보이는가
- 작성 중이던 답안 초안이 보이는가
- 마지막으로 학습하던 트랙·카테고리·문제 위치가 유지되는가
- 새 문제나 변경된 학습 내용이 배포 안내와 일치하는가

문제가 있으면 새 폴더에서 다음 명령으로 서비스를 종료하고, 기존 폴더와 `.env`를 그대로
보존한 채 배포자에게 문의하세요.

```bash
docker compose down
```

## 사용자 수정 파일은 어떻게 하나요?

새 폴더 설치 방식은 사용자가 수정한 파일을 자동으로 합치지 않습니다. 대신 수정본은
기존 보관 폴더에 그대로 남아 있어 손실되지 않습니다.

새 버전이 정상 동작하는 것을 확인한 뒤 필요한 수정만 파일별로 비교해 다시 적용하세요. 기존
폴더 전체를 새 폴더 위에 복사하면 삭제된 구버전 파일과 새 파일이 섞일 수 있으므로 권장하지
않습니다.

## 이전 버전으로 돌아갈 수 있나요?

기존 폴더는 파일과 `.env` 손실을 막기 위한 보관본일 뿐입니다. 새 버전이 Flyway migration을
적용하거나 문제 계약을 변경했다면 이전 프로그램이 새 데이터베이스를 읽지 못할 수 있으므로,
기존 폴더가 있다고 해서 다운그레이드가 자동으로 보장되지는 않습니다.

배포 안내에서 해당 버전의 다운그레이드를 명시적으로 지원할 때만 안내된 절차를 따르세요.
지원 여부가 없다면 volume을 삭제하거나 이전 버전을 임의로 시작하지 말고 먼저 문의하세요.
